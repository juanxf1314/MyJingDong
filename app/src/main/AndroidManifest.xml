<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bwie.juan_mao.jingdong_kanglijuan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".view.user.LoginActivity" />
        <activity android:name=".view.user.RegisterActivity" />
        <activity android:name=".view.user.AccountActivity" />
        <activity android:name=".view.user.PInfoActivity" />
        <activity android:name=".view.user.CNicknameActivity" />
        <activity android:name=".view.address.AddressManageActivity" />
        <activity android:name=".view.address.EditAddressActivity" />
        <activity android:name=".view.address.AddAddressActivity" />
        <activity android:name=".view.shop.ShopListActivity" />
        <activity android:name=".view.shop.SearchActivity" />
        <activity
            android:name=".view.shop.ShopWebActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".view.shop.CartActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".view.shop.OrderActivity" />
        <activity android:name=".view.shop.PlaceOrderActivity"></activity>
    </application>

</manifest>